import { CartScreen } from "@/components/screens/Cart";

export default async function Cart() {
  return (
    <main className="mt-4 flex flex-col 2xl:mt-6 2xl:px-32">
      <CartScreen />
    </main>
  );
}
